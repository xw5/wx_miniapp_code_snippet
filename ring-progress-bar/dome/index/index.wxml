<view class="container">
  <ring-progress-bar
    value="{{0.8}}"
    size="{{110}}"
  />
  <ring-progress-bar
    value="{{0.7}}"
    size="{{180}}"
    baseColor="#ccc"
  />
  <ring-progress-bar
    value="{{per}}"
    size="{{150}}"
    fontSize="{{48}}"
    maxValue="{{500}}"
  />
  <ring-progress-bar
    value="{{per}}"
    size="{{150}}"
    baseColor="#ccc"
    fontSize="{{40}}"
    symbolUnit="%"
  />
</view>